<button class="btn btn-default btn-large action-button" data-wizard="{{wizard}}" data-action="{{action}}" role="button" name="{{name}}" type="{{type}}" value="{{value}}" alt="{{alt}}">{{label}}</button>